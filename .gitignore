# Secrets — never commit real environment values
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Runtime state (kept locally / on the VPS only, not in version control)
src/state.json
src/state.json.tmp

# PM2 / process manager artifacts
.pm2/

# Logs
*.log
npm-debug.log*

# OS/editor cruft
.DS_Store